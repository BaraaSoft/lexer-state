{
  "name": "@lexer-state/arithmetic-lexer",
  "version": "1.0.0",
  "description": "simple arithmetic lexer based on lexer state library written in typescript",
  "author": "Baraa Abuzaid",
  "license": "MIT",
  "private": true,
  "main": "dist/index.js",
  "types": "dist/index.d.js",
  "files": [
    "dist"
  ],
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "yarn tsc --watch --preserveWatchOutput",
    "start": "node ./dist/index.js",
    "build": "yarn run tsc"
  },
  "dependencies": {
    "@lexer-state/machine": "1.0.0"
  },
  "devDependencies": {
    "ts-node": "^10.9.1",
    "typescript": "^5.0.3"
  }
}
